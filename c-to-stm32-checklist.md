# C 语言进入 STM32 前的学习清单

目标：达到“能读懂并修改简单的 STM32 程序”的水平，不要求先把 C 语言全部学完。

## 一、必须掌握

### 1. 基础类型和变量

- [ ] 会定义、初始化和修改变量
- [ ] 理解 `int`、`char`、`float`
- [ ] 知道 `uint8_t`、`uint16_t`、`uint32_t` 表示固定位数的无符号整数
- [ ] 能使用 `printf` 输出变量
- [ ] 能进行加、减、乘、除和取余运算
- [ ] 知道整数除法会舍弃小数部分
- [ ] 知道变量应先初始化再使用

### 2. 条件判断

- [ ] 会使用 `if`、`else if`、`else`
- [ ] 会使用 `switch` 和 `case`
- [ ] 能区分赋值 `=` 与相等比较 `==`
- [ ] 会使用 `>`、`<`、`>=`、`<=`、`==`、`!=`
- [ ] 会使用逻辑与 `&&`
- [ ] 会使用逻辑或 `||`
- [ ] 会使用逻辑非 `!`

### 3. 循环

- [ ] 会使用 `for`
- [ ] 会使用 `while`
- [ ] 理解循环的初始值、继续条件和变量更新
- [ ] 能避免明显的死循环
- [ ] 会使用 `break`
- [ ] 知道 `continue` 的作用
- [ ] 能用循环遍历数组

### 4. 函数

- [ ] 会定义和调用函数
- [ ] 理解函数参数和返回值
- [ ] 知道 `void` 表示无返回值或无参数
- [ ] 能看懂函数声明 `void LED_Init(void);`
- [ ] 知道普通参数传入的是值的副本
- [ ] 知道局部变量只能在所属代码块内使用

### 5. 数组

- [ ] 会定义和初始化数组
- [ ] 知道数组下标从 `0` 开始
- [ ] 知道 5 个元素的下标是 `0` 到 `4`
- [ ] 能用循环读取和输出数组元素
- [ ] 能求数组的总和、平均值和最大值
- [ ] 知道数组越界会造成问题
- [ ] 将数组传给函数时，会同时传入元素数量

## 二、开始 STM32 前需要初步理解

### 6. 指针

- [ ] 知道变量在内存中有地址
- [ ] 知道 `&value` 表示取得 `value` 的地址
- [ ] 会定义简单指针，例如 `int *p;`
- [ ] 知道 `p = &value` 是把变量地址保存到指针中
- [ ] 知道 `*p` 是访问指针所指向的数据
- [ ] 能通过指针参数让函数修改原变量
- [ ] 知道空指针 `NULL`
- [ ] 知道未初始化的指针不能随意解引用
- [ ] 能看懂基本的结构体指针

### 7. 结构体和枚举

- [ ] 会定义简单结构体
- [ ] 会用 `.` 访问结构体成员
- [ ] 会用 `->` 访问结构体指针的成员
- [ ] 知道 `typedef` 可以为类型取名
- [ ] 知道 `enum` 可以表示有限的状态

### 8. 位运算

- [ ] 能看懂二进制和十六进制数字
- [ ] 知道一个字节通常是 8 位
- [ ] 会使用按位与 `&`
- [ ] 会使用按位或 `|`
- [ ] 知道按位取反 `~`
- [ ] 会使用左移 `<<`
- [ ] 会使用右移 `>>`
- [ ] 会设置某一位
- [ ] 会清除某一位
- [ ] 会检查某一位是否为 `1`

常见写法：

```c
value |= (1U << 3);   /* 将第 3 位设为 1 */
value &= ~(1U << 3);  /* 将第 3 位清为 0 */
```

### 9. `const` 和 `volatile`

- [ ] 知道 `const` 表示不应通过该名称修改数据
- [ ] 知道 `volatile` 表示值可能被硬件或其他执行过程改变
- [ ] 知道硬件寄存器经常使用 `volatile`
- [ ] 知道 `volatile` 不等于线程安全或原子操作

### 10. 多文件程序

- [ ] 知道 `.c` 文件通常放函数实现
- [ ] 知道 `.h` 文件通常放函数声明和类型定义
- [ ] 会使用 `#include`
- [ ] 能看懂头文件保护
- [ ] 大致理解“编译”和“链接”的区别
- [ ] 能区分“函数未声明”和“找不到函数实现”两类错误

## 三、工具能力

- [x] 能在 VS Code 中编辑并保存 C 文件
- [x] 能用 MSVC 编译 C 文件
- [x] 能运行程序
- [x] 能设置断点
- [x] 能单步执行
- [x] 能观察变量值
- [ ] 能根据错误信息找到文件名和行号
- [ ] 能区分编译错误、运行错误和逻辑错误

## 四、Git 能力

- [x] 会用 `git status` 查看状态
- [x] 会用 `git diff` 检查修改
- [x] 会用 `git add` 暂存
- [x] 会用 `git diff --staged` 检查待提交内容
- [x] 会用 `git commit` 创建本地版本
- [x] 会用 `git push` 上传
- [x] 已通过 SSH 成功连接 GitHub
- [x] 已配置 `.gitignore`
- [ ] 确认已经从 Git 管理中移除误提交的编译产物
- [ ] 整理重复的 `.vscode/tasks.json`

## 五、暂时不用学习

- 动态内存 `malloc`、`free`
- 链表
- 递归
- 复杂排序算法
- 文件读写
- 函数指针
- 多线程
- 网络编程
- RTOS

## 当前最短学习路线

```text
完成数组
→ 指针基础
→ 结构体
→ 位运算
→ const 和 volatile
→ 多文件基础
→ 开始 STM32 GPIO 点灯
```

达到第二部分“能看懂并修改简单代码”的程度后，就可以开始 STM32，并在硬件练习中继续巩固 C 语言。
